# app.py
from __future__ import annotations
import streamlit as st


st.set_page_config(layout="wide")  # 🌍 растягивает весь контент
st.set_page_config(page_title="Продажи — отчёты", page_icon="📚", layout="wide")

st.title("📚 Отчётный мини‑сайт")
st.write(
"""
Добро пожаловать! Слева — список отчётов.
Этот сайт построен так, чтобы **сайдбар содержал только названия отчётов**.
Все фильтры/настройки — на страницах с отчётами (внутри контента).
"""
)


st.subheader("Что внутри")
st.markdown(
"""
**01 — Сводка.** Быстрый обзор: период, базовая таблица, минимум фильтров.

**02 — Отчёт по товарам.** Топ/низы по метрике, простая агрегация по товару.

👉 Выбирайте страницы слева. Порядок задаётся префиксами `01_`, `02_` в имени файла.
"""
)